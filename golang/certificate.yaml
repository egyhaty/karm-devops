apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: go-murabba-net
  namespace: default
spec:
  secretName: go-murabba-net-tls
  dnsNames:
    - go.murabba.net
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer