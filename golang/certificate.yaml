apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: golang-tls
  namespace: golang
spec:
  secretName: golang-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - golang.example.com
