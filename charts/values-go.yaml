config:
  app: golang

replicaCount: 3

image:
  repository: egyhaty/golang
  tag: "sha-53b16d1"
  pullPolicy: IfNotPresent

service:
  targetPort: 8080

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 128Mi
