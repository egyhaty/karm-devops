config:
  app: golang

replicaCount: 3

image:
  repository: egyhaty/golang
  tag: "sha-d263a31"
  pullPolicy: IfNotPresent

service:
  targetPort: 8080

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 128Mi

ingress:
  enabled: true
  className: traefik
  hosts:
    - host: go.murabba.net
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: go-murabba-net-tls
      hosts:
        - go.murabba.net
