apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apps-chart.fullname" . }}-test-connection"
  labels:
    {{- include "apps-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apps-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
