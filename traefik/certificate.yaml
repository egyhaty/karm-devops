apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-murabba-net
  namespace: traefik
spec:
  secretName: traefik-murabba-net-tls
  dnsNames:
    - traefik.murabba.net
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
