apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: node-murabba-net
  namespace: default
spec:
  secretName: node-murabba-net-tls
  dnsNames:
    - node.murabba.net
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer