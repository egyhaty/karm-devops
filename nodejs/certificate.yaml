apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nodejs-tls
  namespace: nodejs
spec:
  secretName: nodejs-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - nodejs.example.com
