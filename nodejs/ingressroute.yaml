apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nodejs
  namespace: nodejs
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`nodejs.example.com`)
      kind: Rule
      services:
        - name: nodejs
          port: 80
  tls:
    secretName: nodejs-tls
